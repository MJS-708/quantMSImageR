% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_study.R
\name{run_study}
\alias{run_study}
\title{Run a full DESI-MRM study from a YAML configuration}
\usage{
run_study(config_file)
}
\arguments{
\item{config_file}{Path to the study YAML configuration.}
}
\value{
Invisibly, the list produced by \code{\link[=generate_txt_images]{generate_txt_images()}} for the
study, with a \code{calibrated} element added when the configuration's
\verb{calibration:} block is enabled. Called for its side effects: \code{.txt}
images, the calibrated \code{.RDS} and the HTML reports written under the
configured output paths.
}
\description{
Executes the whole pipeline for a study described by a single YAML file:
loads every acquisition, applies the tissue masks, runs SNR filtering at each
requested threshold, optionally builds calibration models and converts tissue
pixels to estimated amounts, writes per-feature \code{.txt} images, and renders one
HTML report per SNR threshold.
}
\details{
The configuration format is documented by the template shipped with the
package:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{file.show(system.file("config_template.yaml", package = "quantMSImageR"))
}\if{html}{\out{</div>}}

A study can also be run from the command line, which uses this function
internally:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Rscript -e 'quantMSImageR::run_study("config.yaml")'
}\if{html}{\out{</div>}}
}
\examples{
# A minimal study driven from a config, using the bundled synthetic data.
# snr_thresh = 0 skips SNR filtering and the tissue-mask requirement, and
# both outputs are switched off, so this runs without writing a report.
\donttest{
out <- file.path(tempdir(), "qmsi_study")
dir.create(out, showWarnings = FALSE)

cfg <- list(
  study = "example_study",
  paths = list(
    data_path    = system.file("extdata", package = "quantMSImageR"),
    out_path     = out,
    image_dir    = file.path(out, "images"),
    lib_ion_path = system.file("extdata", "example_ion_library.csv",
                               package = "quantMSImageR")
  ),
  samples = list(
    list(neg = "example", sections = list("section01"),
         run_id = "S1", label = "A")
  ),
  parameters = list(snr_thresh = 0),
  output = list(render_report = FALSE, output_txt = FALSE)
)

cfg_file <- file.path(out, "config.yaml")
yaml::write_yaml(cfg, cfg_file)

res <- run_study(cfg_file)
names(res)
}

}
\seealso{
Other workflow:
\code{\link[=generate_txt_images]{generate_txt_images()}},
\code{\link[=run_example]{run_example()}}
}
\concept{workflow}
