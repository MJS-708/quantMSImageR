% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contributionHm.R
\name{contributionHm}
\alias{contributionHm}
\title{Group-mean heatmap with per-sample contribution as opacity}
\usage{
contributionHm(
  MSIobject,
  quant_val = 0.5,
  heatmap_order = NA,
  heatmap_labs = NA,
  feature_split = NULL,
  feature_split_name = "Pathway",
  group_split_name = "Group",
  cell_size = 8,
  max_aspect = 1.5,
  cell_border = "white",
  fontsize = 8,
  palette = c("heatmap2", "heatmap0"),
  group_palette = "hat",
  feature_palette = "reading",
  alpha_floor = 0.6,
  saturate = 0.04,
  na_col = "grey88"
)
}
\arguments{
\item{MSIobject}{A \code{quant_MSImagingExperiment} object.}

\item{quant_val}{Numeric in (0, 1). Quantile of pixel intensities summarised
per feature per sample (default \code{0.5}, the median).}

\item{heatmap_order}{Character vector of run names in the desired column order.
Must match values in \code{pData(MSIobject)$run}. Defaults to \code{NA} (use
discovery order).}

\item{heatmap_labs}{Character vector of display labels, one per entry in
\code{heatmap_order}. Used to create column-split groups. Defaults to \code{NA}
(no splitting).}

\item{feature_split}{Optional character/factor vector of length equal to the
number of features, used to split the heatmap columns into labelled groups.
Typically derived from an ion library metadata column (e.g. \code{Met-1}).
Defaults to \code{NULL} (no splitting).}

\item{feature_split_name}{Display name for the feature-group annotation
legend (e.g. \code{"Met-1"}, \code{"Pathway"}). Defaults to \code{"Pathway"}.}

\item{group_split_name}{Display name for the sample-group annotation legend
(e.g. \code{"Treatment"}, \code{"Group"}). Defaults to \code{"Group"}.}

\item{cell_size}{Numeric. Side of a heatmap cell, in millimetres (default
\code{8}). Giving the body an absolute size is what makes cells square; leaving
it to \code{ComplexHeatmap} stretches them to fill the device, which produces
very oblong cells when there are far more features than samples. Set to
\code{NA} to restore the fill-the-device behaviour.}

\item{max_aspect}{Numeric >= 1. Largest cell width-to-height ratio allowed
when one dimension has many more entries than the other (default \code{1.5}).
Cells stay square until the counts differ by more than four-fold; beyond
that the shorter dimension's cells are widened up to this ratio so the
plotting area is not reduced to a sliver.}

\item{cell_border}{Colour for the line drawn around every cell (default
\code{"white"}), which separates neighbouring cells instead of letting equal
colours merge into one block. \code{NA} draws no border.}

\item{fontsize}{Numeric. Point size for the sample and feature labels and the
annotation names (default \code{8}). \code{ComplexHeatmap} defaults to 12, which
crowds the panel once feature names are long enough to need rotating.}

\item{palette}{Character. Diverging colour ramp for the z-scores:
\code{"heatmap2"} (default, blue-white-red) or \code{"heatmap0"}. See
\code{\link[=quantPalettes]{quantPalettes()}}.}

\item{group_palette, feature_palette}{Character. Qualitative palettes for the
sample-group and feature-group colour bars (defaults \code{"hat"} and
\code{"reading"}). Any \code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} palette name also works.}

\item{alpha_floor}{Numeric in [0, 1). Opacity given to a sample that did not
contribute (default \code{0.6}). Below roughly this a non-contributing sample
starts to look like missing data.}

\item{saturate}{Numeric in (0, 0.5). Fraction of cells allowed to saturate
at the colour limit (default \code{0.04}, i.e. 4\%).}

\item{na_col}{Colour for features that cannot be scored -- no variance
across samples, or missing values (default \code{"grey88"}).}
}
\value{
A \code{ComplexHeatmap::Heatmap} (rows = samples, columns = features),
carrying the opacity key in \code{attr(x, "contribution_legend")}. Pass that to
\code{ComplexHeatmap::draw(annotation_legend_list = )} so the opacity channel
is labelled; the report does this.
}
\description{
The same heatmap as \code{\link[=quantileHm]{quantileHm()}} -- same orientation, same group and
feature colour bars, same square cells -- with only the cell fill changed.
Each cell encodes two things at once:
}
\details{
\describe{
\item{Hue}{the sample's \strong{group} mean for that feature -- what the group
did.}
\item{Opacity}{that \strong{sample's} contribution to the group mean -- who
made it do that.}
}

A plain group-mean heatmap cannot show that a group effect rests on one
replicate. Here it is visible directly: a uniformly solid block is a real
group effect, whereas one opaque tile among faded ones means the mean is
carried by a single sample.
}
\section{How the two channels are computed}{


Values are the per-sample quantile of pixel intensities, as in
\code{\link[=quantileHm]{quantileHm()}}, then z-scored \strong{per feature across all samples}, not
within group -- so a cell reads "high or low for this feature" rather than
"abundant or not". Features with zero variance, and missing values, stay
\code{NA} and are drawn in \code{na_col}; they are never imputed to the row mean.

The group mean of those z-scores is the hue. The contribution is
\verb{z * sign(group mean)}, so positive means the sample moved with its group
and zero or negative means it did not; only the positive part drives
opacity, rescaled onto \verb{[alpha_floor, 1]}. \code{alpha_floor} exists because a
nearly transparent tile reads as missing data rather than as weak agreement.
}

\section{Why there are two colour limits}{


The hue is a group \strong{mean}, and averaging \eqn{n} replicates shrinks it by
roughly \eqn{\sqrt{n}}. Reusing the per-sample z limit for the group mean
therefore produces a washed-out panel that no amount of alpha tuning fixes.
Both limits are computed from the quantity actually drawn, each as the
\code{1 - saturate} quantile of its own absolute values, so a similar small
fraction of cells saturates on each channel and the bulk of the data gets
the full ramp. Expect the two to differ by a factor of two or more. Note
this differs from \code{\link[=quantileHm]{quantileHm()}}, which clips to a fixed [-1, 1].
}

\examples{
p <- system.file("extdata", "example.raw", "section01.RDS",
                 package = "quantMSImageR")
obj <- as(readRDS(p), "quant_MSImagingExperiment")
hm <- contributionHm(obj, quant_val = 0.5,
                      heatmap_order = "section01", heatmap_labs = "A")

}
\seealso{
\code{\link[=quantileHm]{quantileHm()}} for the per-sample view of the same matrix.

Other visualisation:
\code{\link{imageR,quant_MSImagingExperiment-method}},
\code{\link[=quantPalettes]{quantPalettes()}},
\code{\link[=quantileHm]{quantileHm()}}
}
\concept{visualisation}
