% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{quantMSImageR-accessors}
\alias{quantMSImageR-accessors}
\alias{calibrationData}
\alias{calibrationModels}
\alias{calibrationR2}
\alias{calibrationDiagnostics}
\alias{calibrationLevels}
\alias{calibrationMetadata}
\alias{tissueData}
\alias{tissueMatrix}
\alias{calibrationData,quant_MSImagingExperiment-method}
\alias{calibrationData<-}
\alias{calibrationData<-,quant_MSImagingExperiment-method}
\alias{calibrationModels,quant_MSImagingExperiment-method}
\alias{calibrationModels<-}
\alias{calibrationModels<-,quant_MSImagingExperiment-method}
\alias{calibrationDiagnostics,quant_MSImagingExperiment-method}
\alias{calibrationR2,quant_MSImagingExperiment-method}
\alias{calibrationLevels,quant_MSImagingExperiment-method}
\alias{calibrationMetadata,quant_MSImagingExperiment-method}
\alias{tissueData,quant_MSImagingExperiment-method}
\alias{tissueMatrix,quant_MSImagingExperiment-method}
\title{Accessors for calibration and tissue metadata}
\usage{
calibrationData(x)

\S4method{calibrationData}{quant_MSImagingExperiment}(x)

calibrationData(x) <- value

\S4method{calibrationData}{quant_MSImagingExperiment}(x) <- value

calibrationModels(x)

\S4method{calibrationModels}{quant_MSImagingExperiment}(x)

calibrationModels(x) <- value

\S4method{calibrationModels}{quant_MSImagingExperiment}(x) <- value

calibrationDiagnostics(x)

\S4method{calibrationDiagnostics}{quant_MSImagingExperiment}(x)

calibrationR2(x)

\S4method{calibrationR2}{quant_MSImagingExperiment}(x)

calibrationLevels(x)

\S4method{calibrationLevels}{quant_MSImagingExperiment}(x)

calibrationMetadata(x)

\S4method{calibrationMetadata}{quant_MSImagingExperiment}(x)

tissueData(x)

\S4method{tissueData}{quant_MSImagingExperiment}(x)

tissueMatrix(x, which = c("roi", "pixel"))

\S4method{tissueMatrix}{quant_MSImagingExperiment}(x, which = c("roi", "pixel"))
}
\arguments{
\item{x}{A \code{quant_MSImagingExperiment} object.}

\item{value}{Replacement value.}

\item{which}{For \code{tissueMatrix()}, whether to return the per-ROI average
matrix (\code{"roi"}, the default) or the per-pixel matrix (\code{"pixel"}).}
}
\value{
\code{calibrationModels()} a named list of \code{lm} objects, one per calibrated
analyte. \code{calibrationDiagnostics()} a data frame of fit quality, one row per
analyte: \code{feature}, \code{r2}, and an \code{out_of_range} column once \code{\link[=int2conc]{int2conc()}} has
run. \code{calibrationR2()} the \code{feature}/\code{r2} columns of that table only.
\code{calibrationLevels()} the summarised response per calibration level.
\code{calibrationMetadata()} the calibration design supplied by the user.
\code{calibrationData()} the whole \code{calibrationInfo} object.
\code{tissueMatrix()} a data frame of features by pixels or by ROI.
The replacement forms return the modified object.
}
\description{
Extract and replace the contents of a \code{quant_MSImagingExperiment}'s
\code{calibrationInfo} and \code{tissueInfo} slots. Prefer these to reaching into the
slots with \code{@}: the internal representation may change, but these functions
will not.
}
\details{
Note the naming: \code{calibrationInfo()} and \code{tissueInfo()} are the \emph{class
constructors}, so the slot accessors are \code{calibrationData()} and
\code{tissueData()}.

The calibration accessors follow the workflow. \code{\link[=summariseCalLevels]{summariseCalLevels()}}
populates \code{calibrationLevels()}, \code{\link[=createCalCurve]{createCalCurve()}} fills
\code{calibrationModels()} and \code{calibrationR2()}, and \code{\link[=int2conc]{int2conc()}} reads the
models back out. \code{calibrationData()} moves the whole block at once, which is
what carrying fitted models from a standards acquisition to a study object
requires.
}
\examples{
cal_dir <- system.file("extdata", "cal_example.raw", package = "quantMSImageR")
cal <- as(readRDS(file.path(cal_dir, "cal_MSI.RDS")),
          "quant_MSImagingExperiment")
cal_metadata <- read.csv(file.path(cal_dir, "calibration_metadata.csv"))

cal <- summariseCalLevels(cal, cal_metadata, val_slot = "intensity",
                            cal_label = "Cal", id = "identifier")
cal <- createCalCurve(cal, cal_type = "cal")

names(calibrationModels(cal))
calibrationDiagnostics(cal)
head(calibrationLevels(cal))

# Carry the fitted models onto a study acquisition
p <- system.file("extdata", "example.raw", "section01.RDS",
                 package = "quantMSImageR")
study <- as(readRDS(p), "quant_MSImagingExperiment")
calibrationData(study) <- calibrationData(cal)

}
\concept{accessors}
