% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/int2snr.R
\name{int2snr,quant_MSImagingExperiment-method}
\alias{int2snr,quant_MSImagingExperiment-method}
\alias{int2snr}
\title{Calculate background-referenced signal-to-noise ratios}
\usage{
\S4method{int2snr}{quant_MSImagingExperiment}(
  MSIobject,
  val_slot = "intensity",
  pixel_header = "sample_name",
  background = "background_pixels",
  tissue = "tissue_pixels",
  snr_thresh = 3,
  average = c("median", "mean"),
  snr_overrides = NULL,
  no_background = c("error", "all_na", "copy"),
  noise = NULL,
  sample_type = NULL,
  ...
)
}
\arguments{
\item{MSIobject}{A \code{quant_MSImagingExperiment} whose \code{pData()} carries a
background label in the \code{pixel_header} column.}

\item{val_slot}{Character. Spectra slot holding the measured response
(default \code{"intensity"}; use \code{"response"} after \code{\link[=int2response]{int2response()}}).}

\item{pixel_header}{Character. Column of \code{pData()} holding the pixel-type
labels (default \code{"sample_name"}, the imaging convention).}

\item{background}{Character. Value in \code{pixel_header} marking the background
(non-tissue) pixels used to estimate the noise level (default
\code{"background_pixels"}). The historical \code{"noise_pixels"} / \code{"Noise"} labels
are matched too, so masks made with earlier versions keep working.}

\item{tissue}{Character. Value in \code{pixel_header} marking the tissue pixels
SNR is calculated for (default \code{"tissue_pixels"}).}

\item{snr_thresh}{Global minimum SNR to accept (below this value SNR = NA).
Applied to every feature unless overridden by \code{snr_overrides}.}

\item{average}{Character, \code{"median"} (default) or \code{"mean"}: statistic used to
summarise the background-pixel vector per feature.}

\item{snr_overrides}{Optional named numeric vector mapping feature name
(as in \code{fData(MSIobject)$name}) to a feature-specific SNR threshold. A
feature listed here uses its own threshold instead of \code{snr_thresh};
features not listed fall back to \code{snr_thresh}. Default \code{NULL} (all
features use the global threshold).}

\item{no_background}{Character. What to do when \code{pixel_header} contains no
background pixels, so there is nothing to reference the signal against:
\code{"error"} (default), \code{"all_na"} (create the slot, all \code{NA}) or \code{"copy"}
(copy \code{val_slot} into the \code{snr} slot -- note the slot then holds measured
response, not a ratio). \code{"error"} and \code{"all_na"} apply only when
\code{snr_thresh > 0}; with \code{snr_thresh = 0} no filtering was requested, so the
values are copied through regardless.}

\item{noise}{Deprecated alias for \code{background}, kept for backward
compatibility. When supplied it overrides \code{background}.}

\item{sample_type}{Deprecated alias for \code{pixel_header}, kept for backward
compatibility. When supplied it overrides \code{pixel_header}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
The input object with an additional \code{snr} spectra slot. Values below
the selected threshold, and values outside the pixels labelled \code{tissue},
are stored as \code{NA} in that slot. No existing slot is modified.
}
\description{
Expresses each tissue pixel's response as a ratio to the same feature's
response in the background pixels of the same acquisition.
}
\details{
For feature \code{f} and tissue pixel \code{p}, with background-pixel set \code{B}:

\deqn{SNR_{f,p} = I_{f,p} / \mathrm{summary}_{b \in B}(I_{f,b})}

where \code{summary} is the mean or median selected by \code{average}. This is a
background-referenced signal ratio, not the classical analytical definition
based on the standard deviation of the noise.

\code{median} is the default because a background region that clips part of the
tissue, or contains a spot of carryover, shifts a mean far more than a
median. Zero background values are treated as missing and imputed at one
tenth of the smallest non-zero background value for that feature, so a
feature whose background is entirely zero or \code{NA} is skipped and its \code{snr}
stays \code{NA}.

Run this \strong{after} internal-standard normalisation (\code{\link[=int2response]{int2response()}}) when
one is used. A threshold of 3 is a detection-level criterion; a stricter
threshold is advisable before converting a feature to calibrated amounts.
}
\examples{
p <- system.file("extdata", "example.raw", "section01.RDS",
                 package = "quantMSImageR")
obj <- as(readRDS(p), "quant_MSImagingExperiment")

# The defaults match the imaging convention used throughout the package
obj <- int2snr(obj, snr_thresh = 3)
names(spectraData(obj))

}
\seealso{
Other filtering:
\code{\link{applySNR,quant_MSImagingExperiment-method}},
\code{\link{back2NA,quant_MSImagingExperiment-method}},
\code{\link{int2response,quant_MSImagingExperiment-method}},
\code{\link{zero2na,quant_MSImagingExperiment-method}}
}
\concept{filtering}
