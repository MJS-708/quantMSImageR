% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantileHm.R
\name{quantileHm}
\alias{quantileHm}
\title{Quantile heatmap of MSI features across samples}
\usage{
quantileHm(
  MSIobject,
  quant_val,
  heatmap_order = NA,
  heatmap_labs = NA,
  feature_split = NULL,
  feature_split_name = "Pathway",
  group_split_name = "Group",
  cell_size = 8,
  max_aspect = 1.5,
  cell_border = "white",
  fontsize = 8,
  palette = c("heatmap2", "heatmap0"),
  group_palette = "hat",
  feature_palette = "reading",
  row_split = NULL,
  row_split_name = NULL,
  column_split_name = NULL
)
}
\arguments{
\item{MSIobject}{A \code{quant_MSImagingExperiment} object.}

\item{quant_val}{Numeric in (0, 1). Quantile to summarise per feature per
sample (e.g. \code{0.95} for the 95th percentile).}

\item{heatmap_order}{Character vector of run names in the desired column order.
Must match values in \code{pData(MSIobject)$run}. Defaults to \code{NA} (use
discovery order).}

\item{heatmap_labs}{Character vector of display labels, one per entry in
\code{heatmap_order}. Used to create column-split groups. Defaults to \code{NA}
(no splitting).}

\item{feature_split}{Optional character/factor vector of length equal to the
number of features, used to split the heatmap columns into labelled groups.
Typically derived from an ion library metadata column (e.g. \code{Met-1}).
Defaults to \code{NULL} (no splitting).}

\item{feature_split_name}{Display name for the feature-group annotation
legend (e.g. \code{"Met-1"}, \code{"Pathway"}). Defaults to \code{"Pathway"}.}

\item{group_split_name}{Display name for the sample-group annotation legend
(e.g. \code{"Treatment"}, \code{"Group"}). Defaults to \code{"Group"}.}

\item{cell_size}{Numeric. Side of a heatmap cell, in millimetres (default
\code{8}). Giving the body an absolute size is what makes cells square; leaving
it to \code{ComplexHeatmap} stretches them to fill the device, which produces
very oblong cells when there are far more features than samples. Set to
\code{NA} to restore the fill-the-device behaviour.}

\item{max_aspect}{Numeric >= 1. Largest cell width-to-height ratio allowed
when one dimension has many more entries than the other (default \code{1.5}).
Cells stay square until the counts differ by more than four-fold; beyond
that the shorter dimension's cells are widened up to this ratio so the
plotting area is not reduced to a sliver.}

\item{cell_border}{Colour for the line drawn around every cell (default
\code{"white"}), which separates neighbouring cells instead of letting equal
colours merge into one block. \code{NA} draws no border.}

\item{fontsize}{Numeric. Point size for the sample and feature labels and the
annotation names (default \code{8}). \code{ComplexHeatmap} defaults to 12, which
crowds the panel once feature names are long enough to need rotating.}

\item{palette}{Character. Diverging colour ramp for the z-scores:
\code{"heatmap2"} (default, blue-white-red) or \code{"heatmap0"}. See
\code{\link[=quantPalettes]{quantPalettes()}}.}

\item{group_palette, feature_palette}{Character. Qualitative palettes for the
sample-group and feature-group colour bars (defaults \code{"hat"} and
\code{"reading"}). Any \code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} palette name also works.}

\item{row_split, row_split_name, column_split_name}{Deprecated aliases for
\code{feature_split}, \code{feature_split_name} and \code{group_split_name}. They were
named for the axis each grouping landed on before samples and features
swapped axes; supplying them still works.}
}
\value{
A \code{ComplexHeatmap::Heatmap} object (rows = samples, columns =
features), with an absolutely-sized body unless \code{cell_size} is \code{NA}.
}
\description{
For each feature, computes the nth quantile of pixel intensities within each
acquisition run, then z-scores the resulting per-feature profile across
samples (clipped to [-1, 1]) and renders a \code{ComplexHeatmap::Heatmap}.
}
\details{
\strong{Rows are samples and columns are features}, in the order given by
\code{heatmap_order}. Studies normally have more samples than features, so this
puts the long dimension vertically where there is room for it and keeps
sample names horizontally readable.
}
\section{What the colour means}{


Each feature is z-scored \strong{on its own}, across samples: the column is
centred on that feature's mean and divided by its own standard deviation.
The colour therefore says where a sample sits \emph{within that feature's} range,
and the ramp is anchored at 0 so white is the feature's mean.

\strong{Colours are not comparable between features.} Because every column is
scaled by its own spread, a feature whose samples differ by a few percent
fills the same blue-to-red range as one that doubles. Reading across a row
to compare two features is the one thing this heatmap will not support. Use
it to ask "which samples are high for this feature", not "which feature
changed most" -- for the latter, see the fold-change table, or
\code{\link[=contributionHm]{contributionHm()}}, which puts every feature on one shared scale.

The z-scores are clipped to \verb{[-1, 1]} and the ramp spans exactly that, so
any sample more than one standard deviation from its feature's mean is drawn
at full intensity. With a handful of samples per group a clear separation
saturates readily, which is deliberate -- the panel is meant to show the
pattern, not the magnitude -- but it does mean two saturated cells can sit
at very different z-scores.

A feature with no variance across samples has an undefined z-score. It is
currently drawn at the bottom of the ramp rather than as missing, so a
perfectly flat feature reads as uniformly low; treat an entirely
single-coloured column with suspicion and check the underlying values.
}

\section{Why there is no scale argument here}{


\code{\link[=contributionHm]{contributionHm()}} takes \code{scale = "shared"} or \code{"feature"}, choosing whether
features can be compared with each other. This function has no such option,
and the reason is structural rather than an omission.

Comparability needs the coloured quantity to still carry cross-feature
information when the colour scale is applied. Here it does not. Every column
is z-scored to mean 0 and standard deviation 1 \emph{before} anything is drawn, so
a feature whose samples differ by two percent and one that doubles arrive at
the ramp with the same spread. The fixed [-1, 1] limits are shared by
every column, but sharing limits cannot restore what standardising already
removed -- the flattening happens first.

\code{\link[=contributionHm]{contributionHm()}} escapes this because it colours the group \strong{mean} of
those z-scores, which is not renormalised per feature: it sits near 0 when
the groups do not separate and approaches 1 when they separate cleanly, and
that range survives to the colour scale. A shared cap on it therefore means
something.

So: this heatmap for one feature's profile across samples, and
\code{contributionHm(scale = "shared")} when the question is which features
separate the groups. Adding \code{scale} here would offer a comparison the
arithmetic cannot support.
}

\examples{
p <- system.file("extdata", "example.raw", "section01.RDS",
                 package = "quantMSImageR")
obj <- as(readRDS(p), "quant_MSImagingExperiment")
hm <- quantileHm(obj, quant_val = 0.5,
                  heatmap_order = "section01", heatmap_labs = "A")

}
\seealso{
Other visualisation:
\code{\link[=contributionHm]{contributionHm()}},
\code{\link{imageR,quant_MSImagingExperiment-method}},
\code{\link[=quantPalettes]{quantPalettes()}}
}
\concept{visualisation}
