% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind_polarities.R
\name{bind_polarities}
\alias{bind_polarities}
\title{Merge positive- and negative-mode acquisitions from the same tissue area}
\usage{
bind_polarities(pos_obj, neg_obj, label = NULL)
}
\arguments{
\item{pos_obj}{A positive-mode \code{MSImagingExperiment}.}

\item{neg_obj}{A negative-mode \code{MSImagingExperiment} of the same tissue.}

\item{label}{Character. Optional. Written to \code{pData(result)$run}.}
}
\value{
Equivalent to \code{bind_panels(pos_obj, neg_obj, label)}.
}
\description{
A thin convenience wrapper around \code{\link[=bind_panels]{bind_panels()}} for the common
positive/negative-mode pairing. \code{bind_panels()} matches pixels on \verb{(x, y)}
rather than requiring identical pixel grids, which matters because pos/neg
pairs nearly always have slightly different pixel counts due to acquisition
timing.
}
\examples{
# As for bind_panels(), the two inputs must be the same physical area. The
# bundled example is negative-mode only, so this stands in for a pos/neg
# pair by splitting one section's features in two.
p <- system.file("extdata", "example.raw", "section01.RDS",
                 package = "quantMSImageR")
obj <- readRDS(p)

merged <- bind_polarities(obj[1:4, ], obj[5:nrow(fData(obj)), ],
                          label = "SampleA_1")

}
\seealso{
\code{\link[=bind_panels]{bind_panels()}}

Other combining acquisitions:
\code{\link[=align_features]{align_features()}},
\code{\link[=bind_panels]{bind_panels()}},
\code{\link{combine_MSIs,MSImagingExperiment-method}},
\code{\link{setCommonAxis,list-method}}
}
\concept{combining acquisitions}
