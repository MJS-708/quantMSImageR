% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readMRM.R
\name{readMRM}
\alias{readMRM}
\title{Read a Waters DESI-MRM acquisition into an MSImagingExperiment}
\usage{
readMRM(
  name,
  folder,
  lib_ion_path,
  overwrite = TRUE,
  type_header = "Type",
  is_norm_header = "IS_norm",
  mz_tolerance = 0.4,
  ambiguity = c("combine", "error", "warn", "nearest")
)
}
\arguments{
\item{name}{Acquisition name (without the \code{.raw} suffix).}

\item{folder}{Path to the directory that contains the \verb{<name>.raw/} folder.}

\item{lib_ion_path}{Full path to the ion-library CSV. Must contain columns
\code{transition_id}, \code{precursor_mz}, \code{product_mz}, \code{collision_eV}, \code{cone_V},
\code{Polarity}, \code{Type} (\code{"Analyte"} for in-tissue analytes, \code{"IS"} for
internal standards). Additional metadata columns are preserved by
downstream code via \code{\link[=buildFeatureMeta]{buildFeatureMeta()}}.

Measured transitions are matched to it on precursor and product m/z within
\code{mz_tolerance}; a transition the library does not describe is kept, named
by its instrument index and typed \code{"Unknown"}.}

\item{overwrite}{Logical. When \code{TRUE} (default) the raw text files are
re-parsed; when \code{FALSE} and a cached \code{MSImagingExperiment.rds} exists
inside the \code{.raw} folder, it is returned instead.}

\item{type_header}{Character. Name of the ion-library column holding the
feature type -- the values that mark internal standards versus analytes
(default \code{"Type"}). Its contents become \code{fData()$feature_type}, which is what
\code{\link[=int2response]{int2response()}} matches \code{IS_name} against.}

\item{is_norm_header}{Character. Name of the optional ion-library column that
maps each analyte to the internal standard normalising it (default
\code{"IS_norm"}). Carried through to \code{fData()} when present, so that
\code{\link[=int2response]{int2response()}} can use it when a panel has more than one standard.
\code{"None"} (or \code{NULL}), or simply omitting the column, is correct for a
single-standard panel.}

\item{mz_tolerance}{Numeric. Half-width, in Da, within which a measured
precursor and product are taken to be the library's. \strong{Both} must agree;
a row matching on precursor alone is not a match.

The default \code{0.4} is nominal-mass matching, because that is what the
instrument does: a triple quadrupole running MRM selects Q1 and Q3 at unit
resolution, so a product ion written as \code{308.1} and one written as \code{308.3}
are the same measurement and no acquisition can separate them. Matching
more tightly than the instrument resolves invents a distinction that is not
in the data, and makes annotation depend on how many decimal places
somebody typed into the library.

The consequence is that genuinely isobaric compounds now collide -- which
is correct, and is what \code{ambiguity = "combine"} is for. Tighten it only for
a high-resolution method where Q3 really is selective.}

\item{ambiguity}{Character. What to do when a measured transition matches
more than one library entry within \code{mz_tolerance}, which m/z alone cannot
resolve. Isomers sharing a nominal precursor and product are the usual
cause, and at unit resolution they are indistinguishable by definition.

\code{"combine"} (default) names the feature for every entry it matched, joined
with \code{" || "} -- \code{"LTC4 || 14_15-LTC4"}. The joined name says the
measurement is one of these, which is what the data supports; naming it for
one of them asserts more than was measured. Annotation columns come from
the closest entry. \code{"error"} refuses, which is right when a collision means
the library is wrong rather than the chemistry ambiguous; \code{"warn"} takes
the closest and says so; \code{"nearest"} takes the closest quietly.

Two library entries for the \emph{same} compound are redundancy, not ambiguity.
\code{"combine"} reports those in its message so they can be removed from the
library rather than carried as a joined name forever.}
}
\value{
An \code{MSImagingExperiment} with feature metadata joined to the ion
library, ready to pass into \code{\link[=generateTxtImages]{generateTxtImages()}} or
\code{\link[=selectTissuePixels]{selectTissuePixels()}}.
}
\description{
Reads one or more analyte text files from \verb{<.raw>/imaging/}, matches
transitions to the ion library on rounded (precursor, product) m/z, and
constructs a \link[Cardinal:MSImagingExperiment]{Cardinal::MSImagingExperiment}. Handles both the
single-file case (one combined \verb{Analyte 1.txt}, the modern QuanOptimise
default) and the multi-file case (older exports that split transitions
across several \verb{Analyte N.txt} files) transparently.
}
\details{
Results are cached to \verb{<.raw>/MSImagingExperiment.rds}; pass
\code{overwrite = TRUE} (the default) to re-parse from the raw text files.
}
\examples{
# Return a previously parsed acquisition from its cached .rds
folder <- system.file("extdata", package = "quantMSImageR")
lib <- system.file("extdata", "ion_library_pos04.csv",
                   package = "quantMSImageR")
obj <- readMRM("pos04_test", folder = folder, lib_ion_path = lib,
                overwrite = FALSE)

}
\seealso{
Other acquisition:
\code{\link{removeBlankMzs,quant_MSImagingExperiment-method}},
\code{\link[=selectTissuePixels]{selectTissuePixels()}},
\code{\link[=trimMSI]{trimMSI()}}
}
\concept{acquisition}
