% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setClasses.R
\docType{class}
\name{calibrationInfo-class}
\alias{calibrationInfo-class}
\alias{calibrationInfo}
\title{Calibration metadata for an imaging experiment}
\value{
An object of class \code{calibrationInfo}.
}
\description{
Holds everything needed to turn a measured response into an estimated amount:
the calibration design, the summarised response at each level, the fitted
per-analyte models, and their fit quality. It is carried in the
\code{calibrationInfo} slot of a \link{quant_MSImagingExperiment}.
}
\details{
The slots are filled in pipeline order and are empty (zero-row data frames,
empty list) until the corresponding step has run:

\describe{
\item{\code{cal_metadata}}{\code{data.frame}. The calibration design supplied by the
user: one row per (spot, analyte), with columns \code{identifier}, \code{analyte},
\code{amount_pg} and \code{level}. Set by \code{\link[=summarise_cal_levels]{summarise_cal_levels()}}.}
\item{\code{cal_response_data}}{\code{data.frame}. Summarised response per
calibration level, with columns \code{analyte}, \code{pg_perpixel},
\code{response_perpixel} and \code{level}. Set by \code{\link[=summarise_cal_levels]{summarise_cal_levels()}}.}
\item{\code{cal_list}}{\code{list}. One \code{stats::lm} per analyte, of
\code{response_perpixel ~ pg_perpixel}, \strong{named by analyte} so that
\code{\link[=int2conc]{int2conc()}} can match models to features by \code{fData(x)$name}. Set by
\code{\link[=create_cal_curve]{create_cal_curve()}}.}
\item{\code{r2_df}}{\code{data.frame}. One row per analyte, with \code{feature} and \code{r2};
\code{\link[=int2conc]{int2conc()}} adds an \code{out_of_range} column giving the proportion of
pixels extrapolated beyond the calibrated range. Set by
\code{\link[=create_cal_curve]{create_cal_curve()}}.}
}

Invariants checked by the validity method: \code{cal_list} must be named if it is
non-empty, and every name must appear in \code{r2_df$feature} when \code{r2_df} is
populated. An unnamed \code{cal_list} is accepted only when empty -- matching
models to features positionally would silently apply one analyte's curve to
another.

Use the accessors (\code{\link[=calibrationModels]{calibrationModels()}}, \code{\link[=calibrationDiagnostics]{calibrationDiagnostics()}},
\code{\link[=calibrationLevels]{calibrationLevels()}}, \code{\link[=calibrationMetadata]{calibrationMetadata()}}) rather than \code{@}.
}
\section{Slots}{

\describe{
\item{\code{cal_metadata}}{\code{data.frame} of the calibration design.}

\item{\code{cal_list}}{Named \code{list} of fitted \code{lm} models, one per analyte.}

\item{\code{cal_response_data}}{\code{data.frame} of summarised response per level.}

\item{\code{r2_df}}{\code{data.frame} of fit quality per analyte.}
}}

\examples{
# Empty, as attached to a freshly coerced object
ci <- calibrationInfo()
ci

# Populated by the calibration pipeline
cal_dir <- system.file("extdata", "cal_example.raw", package = "quantMSImageR")
cal <- as(readRDS(file.path(cal_dir, "cal_MSI.RDS")),
          "quant_MSImagingExperiment")
cal_metadata <- read.csv(file.path(cal_dir, "calibration_metadata.csv"))
cal <- summarise_cal_levels(cal, cal_metadata, val_slot = "intensity",
                            cal_label = "Cal", id = "identifier")
cal <- create_cal_curve(cal, cal_type = "cal")
calibrationDiagnostics(cal)

}
\seealso{
\link{quant_MSImagingExperiment}, \code{\link[=create_cal_curve]{create_cal_curve()}},
\link{quantMSImageR-accessors}

Other calibration:
\code{\link{create_cal_curve,quant_MSImagingExperiment-method}},
\code{\link{int2conc,quant_MSImagingExperiment-method}},
\code{\link{plot_cal_coverage,quant_MSImagingExperiment-method}},
\code{\link{summarise_cal_levels,quant_MSImagingExperiment-method}}
}
\concept{calibration}
