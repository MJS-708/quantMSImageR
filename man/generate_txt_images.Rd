% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_txt_images.R
\name{generate_txt_images}
\alias{generate_txt_images}
\title{Load, process and optionally export per-feature text-image matrices}
\usage{
generate_txt_images(
  fns,
  data_path,
  image_dir,
  lib_ion_path,
  snr_thresh = 0,
  thresh = 20,
  perc = 97,
  rot_clockwise = 0,
  average_method = "median",
  output_txt = TRUE,
  exclude = NULL,
  rename = NULL,
  ratios = NULL,
  output_ratios = TRUE,
  snr_overrides = NULL,
  is_name = NULL,
  is_mode = "line",
  is_window = 15,
  remove_IS = TRUE,
  type_header = "Type"
)
}
\arguments{
\item{fns}{Character vector of acquisition names (without \code{.raw} suffix)
for single-polarity studies. For dual-polarity studies pass a list where
each element is either a plain string (single acquisition) or a named list
with fields \code{pos}, \code{neg}, \code{label}, and optionally \code{prefix_pos}/\code{prefix_neg}
(see \code{\link[=bind_panels]{bind_panels()}}). \code{run_study.R} builds this list automatically
from the YAML \code{samples} section.}

\item{data_path}{Path to the folder that contains the \code{.raw} acquisition
directories.}

\item{image_dir}{Root output directory. One sub-directory per acquisition
is created inside it.}

\item{lib_ion_path}{Full path to the MRM ion-library CSV. Must contain
columns: \code{transition_id}, \code{precursor_mz}, \code{product_mz}, \code{collision_eV},
\code{cone_V}, \code{Polarity}, \code{Type}.}

\item{snr_thresh}{Numeric scalar \strong{or vector}. One or more minimum
signal-to-noise thresholds; pixels below each threshold are set to \code{NA}.
When a vector is supplied, one complete output directory tree is created
per threshold value. A value of \code{0} skips SNR filtering entirely \emph{and}
removes the requirement for a \code{tissue_pixels.csv} mask -- handy for a
smoke-test pass before ROIs are drawn (default \code{0}).}

\item{thresh}{Numeric. Cold-spot percentile passed to \code{imageR()} as
\code{threshold} (default \code{20}).}

\item{perc}{Numeric. Hot-spot percentile passed to \code{imageR()} as
\code{percentile} (default \code{97}).}

\item{rot_clockwise}{Integer (0-3). Number of 90 deg clockwise rotations
applied via \code{pracma::rot90()} (default \code{0}).}

\item{average_method}{Character, \code{"mean"} or \code{"median"}. Statistic used to
summarise the noise pixel vector in \code{int2snr()}. Applied consistently to
every acquisition (default \code{"median"}).}

\item{output_txt}{Logical. When \code{FALSE}, processing runs but no files are
written to disk (default \code{TRUE}).}

\item{exclude}{Character vector of feature names to drop before processing.
Names must match \code{fData()$name} exactly (default \code{NULL} -- keep all).}

\item{rename}{Named character vector or list mapping old feature names to new
display names, e.g. \code{c("old name" = "new name")}. Applied to all combined
objects after loading; the new names appear in file names and the heatmap
(default \code{NULL} -- no renaming).}

\item{ratios}{Optional list of metabolite ratio pairs. Each element must be
a named list with fields:
\describe{
\item{\code{num}}{Name of the numerator feature (matches \code{fData()$name}
\strong{after} any \code{rename} overrides are applied).}
\item{\code{den}}{Name of the denominator feature.}
\item{\code{label}}{\emph{(optional)} Output filename prefix. Defaults to
\code{"<num>_over_<den>"}.}
}
Ratio images (numerator / denominator, pixel-wise) are written to the
same SNR-filtered subdirectories as the individual ion images, immediately
after them. Pixels where the denominator is zero or either ion is \code{NA} are
set to \code{NA} (default \code{NULL} -- no ratios).}

\item{output_ratios}{Logical. When \code{FALSE}, ratio txt files are not written
even if \code{ratios} pairs are defined. Has no effect when \code{output_txt} is
\code{FALSE} (default \code{TRUE}).}

\item{snr_overrides}{Optional named map of feature name to a feature-specific
SNR threshold. Keys may be original ion-library names \strong{or} post-\code{rename}
display names. Listed features use their own threshold in every report;
all other features use the global \code{snr_thresh}. Ignored for any report
whose global threshold is \code{0} (the mask-free smoke-test pass). Default
\code{NULL} (all features use \code{snr_thresh}).}

\item{is_name}{Character. Value identifying the internal standard in
\code{fData()$analyte} -- that is, the ion library's \code{Type} column, typically
\code{"IS"}. It is \strong{not} a transition name.
When supplied, each acquisition is normalised with \code{\link[=int2response]{int2response()}} before
SNR filtering, and every downstream step -- SNR, background masking, images
and ratios -- works on the resulting \code{response} layer instead of
\code{intensity}. \code{NULL} or \code{"None"} (the default) skips normalisation.}

\item{is_mode}{Character. Level at which the internal standard is
summarised: \code{"line"} (default), \code{"sample"}, \code{"pixel"} or \code{"window"} (a
rolling median over \code{is_window} consecutive pixels along the acquisition
line). See \code{\link[=int2response]{int2response()}}.}

\item{is_window}{Integer. Number of consecutive pixels averaged when
\code{is_mode = "window"} (default \code{15}).}

\item{remove_IS}{Logical. Drop the internal-standard feature after
normalising (default \code{TRUE}).}

\item{type_header}{Character. Ion-library column holding the feature type,
passed to \code{\link[=read_mrm]{read_mrm()}} and matched by \code{is_name} (default \code{"Type"}).}
}
\value{
Invisibly returns a named list:
\describe{
\item{\code{combined}}{Raw combined object (background pixels retained).}
\item{\code{combined_snr}}{SNR-filtered intensity for the \strong{first} threshold
in \code{snr_thresh}; sub-threshold pixels \code{NA}. Provided for
backward compatibility.}
\item{\code{combined_snr_list}}{Named list of SNR-filtered objects, one per
threshold in \code{snr_thresh}, named \code{"snr<value>"} (e.g. \code{"snr3"}).}
\item{\code{combined_NAbackground}}{Raw intensity with background set to \code{NA}.}
}
}
\description{
Reads one or more DESI-MRM acquisitions, applies SNR filtering and
tissue/background separation, and writes per-feature text matrices that
can be imported by imaging software (e.g. MassLynx QuanOptimise overlay
tools). Setting \code{output_txt = FALSE} runs all processing but skips file
writing, which is useful when the caller only needs the processed MSI
objects (e.g. for HTML report rendering).
}
\examples{
\donttest{
# Section-mode run on the bundled synthetic data (no tissue mask needed at
# snr_thresh = 0), returning the processed objects without writing files.
fns <- list(list(neg = "example", section = "section01", label = "A"))
res <- generate_txt_images(
  fns          = fns,
  data_path    = system.file("extdata", package = "quantMSImageR"),
  image_dir    = tempfile(),
  lib_ion_path = system.file("extdata", "example_ion_library.csv",
                             package = "quantMSImageR"),
  snr_thresh   = 0,
  output_txt   = FALSE
)
}

}
\seealso{
\code{\link[=int2snr]{int2snr()}}, \code{\link[=applySNR]{applySNR()}}, \code{\link[=back2NA]{back2NA()}}, \code{\link[=imageR]{imageR()}}

Other workflow:
\code{\link[=print.quant_validation]{print.quant_validation()}},
\code{\link[=run_example]{run_example()}},
\code{\link[=run_study]{run_study()}},
\code{\link[=validate_config]{validate_config()}}
}
\concept{workflow}
