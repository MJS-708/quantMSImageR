% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectTissuePixels.R
\name{selectTissuePixels}
\alias{selectTissuePixels}
\title{Interactively select tissue pixels for an acquisition}
\usage{
selectTissuePixels(
  name,
  data_path,
  lib_ion_path,
  feature = NULL,
  enhance = "histogram",
  overwrite = FALSE,
  preview = TRUE
)
}
\arguments{
\item{name}{Character. Acquisition name, without the \code{.raw} suffix
(e.g. \code{"14Dec_AntibodyStudy_1"}).}

\item{data_path}{Character. Path to the folder that contains the \code{.raw}
acquisition directory.}

\item{lib_ion_path}{Character. Full path to the MRM ion-library CSV.}

\item{feature}{Integer index or character name of the feature to use for
tissue selection.  When \code{NULL} (default), all features are displayed
first and the user is prompted to choose one.}

\item{enhance}{Character. Contrast enhancement passed to \code{Cardinal::image()}.
Default \code{"histogram"}.}

\item{overwrite}{Logical. If \code{TRUE}, an existing \code{tissue_pixels.csv} will
be overwritten.  Default \code{FALSE}.}

\item{preview}{Logical. Draw the saved mask (tissue versus background over
the pixel coordinates) once selection finishes, so it can be checked before
the workflow uses it. Default \code{TRUE}.}
}
\value{
Invisibly returns a data frame with columns \code{x}, \code{y},
\code{tissue_pixels} and \code{background_pixels} (logical), one row per pixel, in
acquisition order.
}
\description{
Loads a DESI-MRM acquisition, displays ion images of all features so the
user can identify which best discriminates tissue from background, then
opens an interactive ROI-selection window on a chosen feature.  The
resulting logical mask is saved as \code{tissue_pixels.csv} inside the
acquisition's \code{.raw} folder, where \code{\link[=generateTxtImages]{generateTxtImages()}} will find it
automatically.
}
\details{
This function must be run \strong{before} the YAML workflow for each acquisition
that does not yet have a \code{tissue_pixels.csv}.  It requires an interactive R
session with a graphics device (i.e. not inside \code{Rscript --no-save}).
}
\examples{
# Requires a graphics device and user input, so it only runs interactively.
if (interactive()) {
  # Step 1 (once per acquisition, before running the study):
  selectTissuePixels(
    name         = "my_acquisition",
    data_path    = "path/to/raw",
    lib_ion_path = "path/to/ion_library.csv"
  )

  # Step 2: run the full workflow from the study config
  runStudy("path/to/study.yaml")
}

}
\seealso{
Other acquisition:
\code{\link[=readMRM]{readMRM()}},
\code{\link{removeBlankMzs,quant_MSImagingExperiment-method}},
\code{\link[=trimMSI]{trimMSI()}}
}
\concept{acquisition}
