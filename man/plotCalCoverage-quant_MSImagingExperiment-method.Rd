% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCalCoverage.R
\name{plotCalCoverage,quant_MSImagingExperiment-method}
\alias{plotCalCoverage,quant_MSImagingExperiment-method}
\alias{plotCalCoverage}
\title{Check that measured pixels fall within the calibrated range}
\usage{
\S4method{plotCalCoverage}{quant_MSImagingExperiment}(
  MSIobject,
  features = NULL,
  val_slot = "intensity",
  conc_slot = "pg_pixel",
  log_base = 2,
  bins = 40,
  cal_colour = "red",
  cal_alpha = 0.4,
  cal_linetype = "dotted",
  ...
)
}
\arguments{
\item{MSIobject}{A \code{quant_MSImagingExperiment} that has been through
\code{\link[=createCalCurve]{createCalCurve()}} and \code{\link[=int2conc]{int2conc()}}, so it carries both the fitted models
(see \code{\link[=calibrationModels]{calibrationModels()}}) and a calibrated-amount slot.}

\item{features}{Features to plot: a character vector of names as in
\code{fData(MSIobject)$name}, or numeric row indices. Defaults to every feature
that has a calibration model.}

\item{val_slot}{Character. Spectra slot holding the measured response
(default \code{"intensity"}).}

\item{conc_slot}{Character. Spectra slot holding the calibrated amount
estimate written by \code{\link[=int2conc]{int2conc()}}. Defaults to \code{"pg_pixel"}; objects
calibrated with earlier versions carry \code{"conc - pg/pixel"}, which is
matched automatically.}

\item{log_base}{Numeric. Base for both axes (default \code{2}).}

\item{bins}{Integer. Number of histogram bins for the pixel distribution
(default \code{40}).}

\item{cal_colour}{Colour used for the standards and the level markers
(default \code{"red"}).}

\item{cal_alpha}{Numeric. Opacity of the calibration-level marker lines
(default \code{0.4}), kept faint so they read as a reference grid rather than
competing with the data.}

\item{cal_linetype}{Line type for the calibration-level markers (default
\code{"dotted"}).}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{patchwork} object: the pixel histogram stacked above the
calibration curve, sharing an x-axis. Printing it draws the figure; it can
also be further modified with \code{ggplot2} layers.
}
\description{
Converting a response into a calibrated amount is only trustworthy where the
standards actually constrain the fit. A calibration whose levels sit above or
below the signal measured in tissue is extrapolation, not quantification.
\code{\link[=int2conc]{int2conc()}} reports the proportion of extrapolated pixels per feature, but
without an explicit coverage check that is still easy to overlook -- and the
plot shows \emph{where} the mismatch is, not just how much of it there is.
}
\details{
This function plots the two together on log axes so the overlap can be
judged directly. The lower panel is the calibration itself: the standard
spots and the fitted model that \code{int2conc()} inverts. The upper panel is the
distribution of the object's own pixels on the same x-axis, with the
calibration levels marked. When the histogram sits inside the span of those
marks, every converted pixel is interpolated between real standards.

The standards are read back from each model's own model frame
(\code{stats::model.frame}) rather than from \code{cal_response_data}. This matters for
\code{cal_type = "std_addition"}, where \code{\link[=createCalCurve]{createCalCurve()}} shifts the amount
axis by the estimated endogenous amount before fitting: the stored response
data is on the unshifted axis, so plotting it against the fitted line would
put the points and the model on different scales. The model frame always
holds the points the line was actually fitted to.

The fitted model is linear in amount, so on log axes it is drawn as a curve
evaluated across the plotted range rather than as a straight line. Any part
of it predicting a non-positive response has no log-scale counterpart and is
dropped.
}
\examples{
cal_dir <- system.file("extdata", "cal_example.raw", package = "quantMSImageR")
cal <- as(readRDS(file.path(cal_dir, "cal_MSI.RDS")),
          "quant_MSImagingExperiment")
cal_metadata <- read.csv(file.path(cal_dir, "calibration_metadata.csv"))
cal <- summariseCalLevels(cal, cal_metadata, val_slot = "intensity",
                            cal_label = "Cal", id = "identifier")
cal <- createCalCurve(cal, cal_type = "cal")
cal <- int2conc(cal, pixel_header = "sample_type", pixels = "Tissue")

plotCalCoverage(cal, val_slot = "intensity")

}
\seealso{
Other calibration:
\code{\link{calibrationInfo-class}},
\code{\link{createCalCurve,quant_MSImagingExperiment-method}},
\code{\link{int2conc,quant_MSImagingExperiment-method}},
\code{\link{summariseCalLevels,quant_MSImagingExperiment-method}}
}
\concept{calibration}
