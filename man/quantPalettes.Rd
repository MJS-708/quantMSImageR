% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{quantPalettes}
\alias{quantPalettes}
\title{Colour palettes used by quantMSImageR}
\usage{
quantPalettes(name = NULL, n = NULL)
}
\arguments{
\item{name}{Optional palette name. When \code{NULL} (default) the whole list is
returned.}

\item{n}{Optional number of colours. Qualitative palettes are truncated,
continuous ones are interpolated. \code{NULL} (default) returns the palette at
its native length.}
}
\value{
A named list of character vectors, or a single character vector of
hex colours when \code{name} is given.
}
\description{
The palettes available to \code{\link[=imageR]{imageR()}}, \code{\link[=quantileHm]{quantileHm()}} and the metadata
colour bars, returned as named character vectors of hex colours.
}
\details{
Four of these -- \code{heatmap0}, \code{heatmap2}, \code{hat} and \code{reading} -- are taken
from the \code{ltc} package by Loukas Theodosiou
(\url{https://github.com/loukesio/ltc-color-palettes}), which is MIT licensed.
\code{heatmap2} and \code{hat} are re-ordered relative to ltc -- see below -- and
\code{hat} substitutes a grey for ltc's black; the remaining colours are
unchanged.
They are reproduced here rather than depended on: \code{ltc} brings in 34
recursive dependencies, which is a large amount of installation surface for
four colour vectors in a package that is otherwise light.

Which palette suits what:
\describe{
\item{\code{viridis}}{Sequential, perceptually uniform and colour-blind safe.
The default for ion images, where the quantity is one-directional.}
\item{\code{heatmap0}}{Nine-colour sequential ramp running dark blue to teal to
sand to red. An alternative for ion images.}
\item{\code{heatmap2}}{Five-colour diverging ramp, blue to white to red. Suits
z-scores, where the midpoint is meaningful.}
\item{\code{hat}, \code{reading}}{Qualitative, for categorical metadata such as study
group or pathway class. \code{hat} carries 10 well-separated hues, re-sequenced
from ltc's ordering so that the first few are maximally distinct, with
ltc's black replaced by a neutral grey placed last; \code{reading} is a softer
8-colour set.}
}
}
\examples{
names(quantPalettes())
quantPalettes("heatmap2")
quantPalettes("hat", n = 3)

}
\seealso{
Other visualisation:
\code{\link[=contributionHm]{contributionHm()}},
\code{\link{imageR,quant_MSImagingExperiment-method}},
\code{\link[=quantileHm]{quantileHm()}}
}
\concept{visualisation}
