% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_config}
\alias{validate_config}
\title{Validate a study YAML configuration}
\usage{
validate_config(config, check_paths = TRUE)
}
\arguments{
\item{config}{A config list, or a path to a study YAML file.}

\item{check_paths}{Logical. Also check that the referenced data directory,
ion library and calibration files exist on disk (default \code{TRUE}). Set
\code{FALSE} to validate a config's structure away from its data.}
}
\value{
A \code{quant_validation} object with \code{errors}, \code{warnings} and a \code{summary}
data frame of every check.
}
\description{
Structural and cross-file checks on a study config before anything is
processed: required blocks present, referenced files and ion-library columns
actually exist, enumerated values spelled correctly, and the calibration and
internal-standard settings coherent with each other.
}
\details{
Every check runs, so one call reports every problem rather than stopping at
the first. \code{\link[=run_study]{run_study()}} calls this first and refuses to start if there are
errors -- these are mistakes that would otherwise surface as an obscure
failure halfway through a long run, or worse, as a plausible-looking result
computed from the wrong column.
}
\examples{
# The shipped template is structurally valid; its placeholder paths are not
# meant to exist, so the path checks are skipped here.
cfg <- system.file("config_template.yaml", package = "quantMSImageR")
validate_config(cfg, check_paths = FALSE)

}
\seealso{
Other workflow:
\code{\link[=generate_txt_images]{generate_txt_images()}},
\code{\link[=print.quant_validation]{print.quant_validation()}},
\code{\link[=run_example]{run_example()}},
\code{\link[=run_study]{run_study()}}
}
\concept{workflow}
